<?xml version="1.0" encoding="UTF-8"?>
<testsuite
  name="soft_mux.uncrustify"
  tests="3"
  failures="3"
  errors="0"
  time="0.074"
>
  <testcase
    name="include/soft_mux.hpp"
    classname="soft_mux.uncrustify"
  >
      <failure message="Diff with 103 lines"><![CDATA[--- include/soft_mux.hpp
+++ include/soft_mux.hpp.uncrustify
@@ -12,40 +12,45 @@
-    public:
-        friend class TestSoftMuxInterface_MuxSendCtrlPwms_Test;
-        friend class TestSoftMuxInterface_MuxSendCliPwms_Test;
-        friend class TestSoftMuxInterface_MuxTestSetModeService_Test;
-        friend class TestSoftMuxInterface_MuxTestGetModeService_Test;
-        friend class TestSoftMuxInterface_MuxTestInputHB_Test;
-        friend class TestSoftMuxInterface_NoCtrlHeartbeatTriggersStop_Test;
-        friend class TestSoftMuxInterface_NoCliHeartbeatTriggersStop_Test;
-        friend class TestSoftMuxInterface_NoCtrlHeartbeatonCli_Test;
-        friend class TestSoftMuxInterface_NoCliHeartbeatonCtrl_Test;
-        friend class TestSoftMuxInterface_MuxHeartbeat_Test;
-        SoftMux();
-        void pwm_ctrl_callback(custom_interfaces::msg::Pwms::UniquePtr pwm);
-        void pwm_cli_callback(custom_interfaces::msg::Pwms::UniquePtr pwm);
-        void set_mode_srv(const std::shared_ptr<std_srvs::srv::SetBool::Request> request, std::shared_ptr<std_srvs::srv::SetBool::Response> response);
-        void pub_mode_srv(const std::shared_ptr<std_srvs::srv::SetBool::Request> request, std::shared_ptr<std_srvs::srv::SetBool::Response> response);
-    private:
-        void ctrl_heartbeat_callback(std_msgs::msg::Bool::UniquePtr heartbeat);
-        void ctrl_heartbeat_check_callback();
-        void cli_heartbeat_callback(std_msgs::msg::Bool::UniquePtr heartbeat);
-        void cli_heartbeat_check_callback();
-        void pwm_cmd_publish(custom_interfaces::msg::Pwms::UniquePtr pwm);
-        void publish_stop_command();
-        void mux_heartbeat_send();
-       
-        rclcpp::Service<std_srvs::srv::SetBool>::SharedPtr control_mode;
-        rclcpp::Service<std_srvs::srv::SetBool>::SharedPtr force_pub;
-       
-        rclcpp::Subscription<custom_interfaces::msg::Pwms>::SharedPtr pwm_ctrl_subscriber;
-        rclcpp::Subscription<custom_interfaces::msg::Pwms>::SharedPtr pwm_cli_subscriber;
-        rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr ctrl_heartbeat_subscriber;
-        rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr cli_heartbeat_subscriber;
-       
-        rclcpp::Publisher<custom_interfaces::msg::Pwms>::SharedPtr pwm_cmd_publisher;
-        rclcpp::Publisher<std_msgs::msg::Bool>::SharedPtr current_control_mode;
-        rclcpp::Publisher<std_msgs::msg::Bool>::SharedPtr mux_heartbeat_publisher;
-       
-        rclcpp::TimerBase::SharedPtr ctrl_heartbeat_timer;
-        rclcpp::TimerBase::SharedPtr cli_heartbeat_timer;
-        rclcpp::TimerBase::SharedPtr mux_heartbeat_timer;
+public:
+  friend class TestSoftMuxInterface_MuxSendCtrlPwms_Test;
+  friend class TestSoftMuxInterface_MuxSendCliPwms_Test;
+  friend class TestSoftMuxInterface_MuxTestSetModeService_Test;
+  friend class TestSoftMuxInterface_MuxTestGetModeService_Test;
+  friend class TestSoftMuxInterface_MuxTestInputHB_Test;
+  friend class TestSoftMuxInterface_NoCtrlHeartbeatTriggersStop_Test;
+  friend class TestSoftMuxInterface_NoCliHeartbeatTriggersStop_Test;
+  friend class TestSoftMuxInterface_NoCtrlHeartbeatonCli_Test;
+  friend class TestSoftMuxInterface_NoCliHeartbeatonCtrl_Test;
+  friend class TestSoftMuxInterface_MuxHeartbeat_Test;
+  SoftMux();
+  void pwm_ctrl_callback(custom_interfaces::msg::Pwms::UniquePtr pwm);
+  void pwm_cli_callback(custom_interfaces::msg::Pwms::UniquePtr pwm);
+  void set_mode_srv(
+    const std::shared_ptr<std_srvs::srv::SetBool::Request> request,
+    std::shared_ptr<std_srvs::srv::SetBool::Response> response);
+  void pub_mode_srv(
+    const std::shared_ptr<std_srvs::srv::SetBool::Request> request,
+    std::shared_ptr<std_srvs::srv::SetBool::Response> response);
+
+private:
+  void ctrl_heartbeat_callback(std_msgs::msg::Bool::UniquePtr heartbeat);
+  void ctrl_heartbeat_check_callback();
+  void cli_heartbeat_callback(std_msgs::msg::Bool::UniquePtr heartbeat);
+  void cli_heartbeat_check_callback();
+  void pwm_cmd_publish(custom_interfaces::msg::Pwms::UniquePtr pwm);
+  void publish_stop_command();
+  void mux_heartbeat_send();
+
+  rclcpp::Service<std_srvs::srv::SetBool>::SharedPtr control_mode;
+  rclcpp::Service<std_srvs::srv::SetBool>::SharedPtr force_pub;
+
+  rclcpp::Subscription<custom_interfaces::msg::Pwms>::SharedPtr pwm_ctrl_subscriber;
+  rclcpp::Subscription<custom_interfaces::msg::Pwms>::SharedPtr pwm_cli_subscriber;
+  rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr ctrl_heartbeat_subscriber;
+  rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr cli_heartbeat_subscriber;
+
+  rclcpp::Publisher<custom_interfaces::msg::Pwms>::SharedPtr pwm_cmd_publisher;
+  rclcpp::Publisher<std_msgs::msg::Bool>::SharedPtr current_control_mode;
+  rclcpp::Publisher<std_msgs::msg::Bool>::SharedPtr mux_heartbeat_publisher;
+
+  rclcpp::TimerBase::SharedPtr ctrl_heartbeat_timer;
+  rclcpp::TimerBase::SharedPtr cli_heartbeat_timer;
+  rclcpp::TimerBase::SharedPtr mux_heartbeat_timer;
@@ -54,2 +59,2 @@
-        std::chrono::time_point<std::chrono::steady_clock> recent_ctrl_heartbeat;
-        std::chrono::time_point<std::chrono::steady_clock> recent_cli_heartbeat;
+  std::chrono::time_point<std::chrono::steady_clock> recent_ctrl_heartbeat;
+  std::chrono::time_point<std::chrono::steady_clock> recent_cli_heartbeat;
@@ -58,3 +63,3 @@
-        bool is_matlab_mode;
-        bool no_ctrl_heartbeat;
-        bool no_cli_heartbeat;
+  bool is_matlab_mode;
+  bool no_ctrl_heartbeat;
+  bool no_cli_heartbeat;
@@ -62,2 +66,0 @@
-
-
]]></failure>
  </testcase>
  <testcase
    name="src/soft_mux.cpp"
    classname="soft_mux.uncrustify"
  >
      <failure message="Diff with 257 lines"><![CDATA[--- src/soft_mux.cpp
+++ src/soft_mux.cpp.uncrustify
@@ -7 +7,3 @@
-SoftMux::SoftMux() : rclcpp::Node("SoftMux"), is_matlab_mode(false), no_ctrl_heartbeat(true), no_cli_heartbeat(true) {
+SoftMux::SoftMux()
+: rclcpp::Node("SoftMux"), is_matlab_mode(false), no_ctrl_heartbeat(true), no_cli_heartbeat(true)
+{
@@ -9,4 +11,8 @@
-    pwm_ctrl_subscriber = this->create_subscription<custom_interfaces::msg::Pwms>("pwm_ctrl", 10, std::bind(&SoftMux::pwm_ctrl_callback, this, std::placeholders::_1));
-    pwm_cli_subscriber =  this->create_subscription<custom_interfaces::msg::Pwms>("pwm_cli", 10, std::bind(&SoftMux::pwm_cli_callback, this, std::placeholders::_1));
-    ctrl_heartbeat_subscriber = this->create_subscription<std_msgs::msg::Bool>("ctrl_heartbeat", 10, std::bind(&SoftMux::ctrl_heartbeat_callback, this, std::placeholders::_1));
-    cli_heartbeat_subscriber= this->create_subscription<std_msgs::msg::Bool>("cli_heartbeat",10, std::bind(&SoftMux::cli_heartbeat_callback, this, std::placeholders::_1));
+  pwm_ctrl_subscriber = this->create_subscription<custom_interfaces::msg::Pwms>("pwm_ctrl", 10,
+    std::bind(&SoftMux::pwm_ctrl_callback, this, std::placeholders::_1));
+  pwm_cli_subscriber = this->create_subscription<custom_interfaces::msg::Pwms>("pwm_cli", 10,
+    std::bind(&SoftMux::pwm_cli_callback, this, std::placeholders::_1));
+  ctrl_heartbeat_subscriber = this->create_subscription<std_msgs::msg::Bool>("ctrl_heartbeat", 10,
+    std::bind(&SoftMux::ctrl_heartbeat_callback, this, std::placeholders::_1));
+  cli_heartbeat_subscriber = this->create_subscription<std_msgs::msg::Bool>("cli_heartbeat", 10,
+    std::bind(&SoftMux::cli_heartbeat_callback, this, std::placeholders::_1));
@@ -16,4 +22,7 @@
-    ctrl_heartbeat_timer = this->create_wall_timer(500ms, std::bind(&SoftMux::ctrl_heartbeat_check_callback, this));
-    cli_heartbeat_timer = this->create_wall_timer(500ms, std::bind(&SoftMux::cli_heartbeat_check_callback, this));
-    mux_heartbeat_timer = this->create_wall_timer(500ms, std::bind(&SoftMux::mux_heartbeat_send, this));
-   
+  ctrl_heartbeat_timer = this->create_wall_timer(500ms,
+    std::bind(&SoftMux::ctrl_heartbeat_check_callback, this));
+  cli_heartbeat_timer = this->create_wall_timer(500ms,
+    std::bind(&SoftMux::cli_heartbeat_check_callback, this));
+  mux_heartbeat_timer = this->create_wall_timer(500ms,
+    std::bind(&SoftMux::mux_heartbeat_send, this));
+
@@ -21,2 +30,4 @@
-    control_mode = this->create_service<std_srvs::srv::SetBool>("control_mode", std::bind(&SoftMux::set_mode_srv, this, std::placeholders::_1, std::placeholders::_2));
-    force_pub = this->create_service<std_srvs::srv::SetBool>("force_pub", std::bind(&SoftMux::pub_mode_srv, this, std::placeholders::_1, std::placeholders::_2));
+  control_mode = this->create_service<std_srvs::srv::SetBool>("control_mode",
+    std::bind(&SoftMux::set_mode_srv, this, std::placeholders::_1, std::placeholders::_2));
+  force_pub = this->create_service<std_srvs::srv::SetBool>("force_pub",
+    std::bind(&SoftMux::pub_mode_srv, this, std::placeholders::_1, std::placeholders::_2));
@@ -26,4 +37,4 @@
-    pwm_cmd_publisher = this->create_publisher<custom_interfaces::msg::Pwms>("pwm_cmd", 10);
-    current_control_mode = this->create_publisher<std_msgs::msg::Bool>("current_mode", 10);
-    mux_heartbeat_publisher = this->create_publisher<std_msgs::msg::Bool>("mux_heartbeat", 10);
-     
+  pwm_cmd_publisher = this->create_publisher<custom_interfaces::msg::Pwms>("pwm_cmd", 10);
+  current_control_mode = this->create_publisher<std_msgs::msg::Bool>("current_mode", 10);
+  mux_heartbeat_publisher = this->create_publisher<std_msgs::msg::Bool>("mux_heartbeat", 10);
+
@@ -33,9 +44,8 @@
-
-
-void SoftMux::pwm_ctrl_callback(custom_interfaces::msg::Pwms::UniquePtr pwm) {
-    if (no_ctrl_heartbeat) {
-        return;
-    }
-    if (is_matlab_mode) {
-        pwm_cmd_publish(std::move(pwm));
-    }
+void SoftMux::pwm_ctrl_callback(custom_interfaces::msg::Pwms::UniquePtr pwm)
+{
+  if (no_ctrl_heartbeat) {
+    return;
+  }
+  if (is_matlab_mode) {
+    pwm_cmd_publish(std::move(pwm));
+  }
@@ -45,7 +55,8 @@
-void SoftMux::pwm_cli_callback(custom_interfaces::msg::Pwms::UniquePtr pwm) {
-    if (no_cli_heartbeat) {
-        return;
-    }
-    if (!is_matlab_mode) {
-        pwm_cmd_publish(std::move(pwm));
-    }
+void SoftMux::pwm_cli_callback(custom_interfaces::msg::Pwms::UniquePtr pwm)
+{
+  if (no_cli_heartbeat) {
+    return;
+  }
+  if (!is_matlab_mode) {
+    pwm_cmd_publish(std::move(pwm));
+  }
@@ -55,4 +66,14 @@
-void SoftMux::set_mode_srv(const std::shared_ptr<std_srvs::srv::SetBool::Request> request, std::shared_ptr<std_srvs::srv::SetBool::Response> response) {
-    bool mode_change = false;
-    if (this->is_matlab_mode != request->data) {
-        mode_change = true;
+void SoftMux::set_mode_srv(
+  const std::shared_ptr<std_srvs::srv::SetBool::Request> request,
+  std::shared_ptr<std_srvs::srv::SetBool::Response> response)
+{
+  bool mode_change = false;
+  if (this->is_matlab_mode != request->data) {
+    mode_change = true;
+  }
+  this->is_matlab_mode = request->data;
+  if (this->is_matlab_mode == request->data) {
+    if (mode_change) {
+      auto message = std_msgs::msg::Bool();
+      message.data = this->is_matlab_mode;
+      this->current_control_mode->publish(message);
@@ -60,11 +81,4 @@
-    this->is_matlab_mode = request->data;
-    if (this->is_matlab_mode == request->data) {
-        if (mode_change) {
-            auto message = std_msgs::msg::Bool();
-            message.data = this->is_matlab_mode;
-            this->current_control_mode->publish(message);
-        }
-        response->success = true;
-    } else {
-        response->success = false;
-    }
+    response->success = true;
+  } else {
+    response->success = false;
+  }
@@ -73,4 +87,7 @@
-void SoftMux::pub_mode_srv(const std::shared_ptr<std_srvs::srv::SetBool::Request> request, std::shared_ptr<std_srvs::srv::SetBool::Response> response) {
-    auto message = std_msgs::msg::Bool();
-    message.data = this->is_matlab_mode;
-    this->current_control_mode->publish(message);
+void SoftMux::pub_mode_srv(
+  const std::shared_ptr<std_srvs::srv::SetBool::Request> request,
+  std::shared_ptr<std_srvs::srv::SetBool::Response> response)
+{
+  auto message = std_msgs::msg::Bool();
+  message.data = this->is_matlab_mode;
+  this->current_control_mode->publish(message);
@@ -78 +95 @@
-    response->success = true;
+  response->success = true;
@@ -80 +97 @@
-    (void) request; // stop compiler complaining about unused variables
+  (void) request;   // stop compiler complaining about unused variables
@@ -84,3 +101,3 @@
-
-void SoftMux::pwm_cmd_publish(custom_interfaces::msg::Pwms::UniquePtr pwm) {
-    this->pwm_cmd_publisher->publish(*(std::move(pwm)));
+void SoftMux::pwm_cmd_publish(custom_interfaces::msg::Pwms::UniquePtr pwm)
+{
+  this->pwm_cmd_publisher->publish(*(std::move(pwm)));
@@ -90,6 +107,7 @@
-void SoftMux::ctrl_heartbeat_callback(std_msgs::msg::Bool::UniquePtr heartbeat) {
-    recent_ctrl_heartbeat = std::chrono::steady_clock::now();
-    if (no_ctrl_heartbeat) {
-        no_ctrl_heartbeat = false; // If we just received a heartbeat, then we certainly have a heartbeat!
-    }
-    (void) heartbeat; // stop compiler complaining about unused variables
+void SoftMux::ctrl_heartbeat_callback(std_msgs::msg::Bool::UniquePtr heartbeat)
+{
+  recent_ctrl_heartbeat = std::chrono::steady_clock::now();
+  if (no_ctrl_heartbeat) {
+    no_ctrl_heartbeat = false;     // If we just received a heartbeat, then we certainly have a heartbeat!
+  }
+  (void) heartbeat;   // stop compiler complaining about unused variables
@@ -99,11 +117,10 @@
-void SoftMux::ctrl_heartbeat_check_callback() {
-    if (is_matlab_mode) {
-        auto current_time = std::chrono::steady_clock::now();
-        if (current_time - recent_ctrl_heartbeat > 1s) {
-            RCLCPP_INFO(this->get_logger(), "Didn't get heartbeat from ctrl. Sending stop command.");
-            no_ctrl_heartbeat = true;
-            publish_stop_command();
-        }
-        else {
-            no_ctrl_heartbeat = false;
-        }
+void SoftMux::ctrl_heartbeat_check_callback()
+{
+  if (is_matlab_mode) {
+    auto current_time = std::chrono::steady_clock::now();
+    if (current_time - recent_ctrl_heartbeat > 1s) {
+      RCLCPP_INFO(this->get_logger(), "Didn't get heartbeat from ctrl. Sending stop command.");
+      no_ctrl_heartbeat = true;
+      publish_stop_command();
+    } else {
+      no_ctrl_heartbeat = false;
@@ -110,0 +128 @@
+  }
@@ -112,4 +130,18 @@
-void SoftMux::cli_heartbeat_callback(std_msgs::msg::Bool::UniquePtr heartbeat){
-    recent_cli_heartbeat = std::chrono::steady_clock::now();
-    if (no_cli_heartbeat) {
-        no_cli_heartbeat = false; // If we just received a heartbeat, then we certainly have a heartbeat!
+void SoftMux::cli_heartbeat_callback(std_msgs::msg::Bool::UniquePtr heartbeat)
+{
+  recent_cli_heartbeat = std::chrono::steady_clock::now();
+  if (no_cli_heartbeat) {
+    no_cli_heartbeat = false;     // If we just received a heartbeat, then we certainly have a heartbeat!
+  }
+  (void) heartbeat;   // stop compiler complaining about unused variables
+}
+void SoftMux::cli_heartbeat_check_callback()
+{
+  if (!is_matlab_mode) {
+    auto current_time = std::chrono::steady_clock::now();
+    if (current_time - recent_cli_heartbeat > 1s) {
+      RCLCPP_INFO(this->get_logger(), "Didn't get heartbeat from cli. Sending stop command.");
+      no_cli_heartbeat = true;
+      publish_stop_command();
+    } else {
+      no_cli_heartbeat = false;
@@ -117,14 +149 @@
-    (void) heartbeat; // stop compiler complaining about unused variables
-}
-void SoftMux::cli_heartbeat_check_callback() {
-    if (!is_matlab_mode) {
-        auto current_time = std::chrono::steady_clock::now();
-        if (current_time - recent_cli_heartbeat > 1s) {
-            RCLCPP_INFO(this->get_logger(), "Didn't get heartbeat from cli. Sending stop command.");
-            no_cli_heartbeat = true;
-            publish_stop_command();
-        }
-        else {
-            no_cli_heartbeat = false;
-        }
-    }  
+  }
@@ -134,3 +153,4 @@
-void SoftMux::mux_heartbeat_send() {
-    std_msgs::msg::Bool msg;
-    this->mux_heartbeat_publisher->publish(msg);
+void SoftMux::mux_heartbeat_send()
+{
+  std_msgs::msg::Bool msg;
+  this->mux_heartbeat_publisher->publish(msg);
@@ -140,6 +160,7 @@
-void SoftMux::publish_stop_command() {
-    auto stop_pwms = custom_interfaces::msg::Pwms();
-    for (int i = 0; i < 8; i++) {
-        stop_pwms.pwms[i] = 1500;
-    }
-    pwm_cmd_publish(std::make_unique<custom_interfaces::msg::Pwms>(stop_pwms));
+void SoftMux::publish_stop_command()
+{
+  auto stop_pwms = custom_interfaces::msg::Pwms();
+  for (int i = 0; i < 8; i++) {
+    stop_pwms.pwms[i] = 1500;
+  }
+  pwm_cmd_publish(std::make_unique<custom_interfaces::msg::Pwms>(stop_pwms));
@@ -150,6 +171,7 @@
-    int main(int argc, char* argv[]) {
-        rclcpp::init(argc, argv);
-        rclcpp::spin(std::make_shared<SoftMux>());
-        rclcpp::shutdown();
-        return 0;
-    }
+int main(int argc, char * argv[])
+{
+  rclcpp::init(argc, argv);
+  rclcpp::spin(std::make_shared<SoftMux>());
+  rclcpp::shutdown();
+  return 0;
+}
@@ -157 +178,0 @@
-
]]></failure>
  </testcase>
  <testcase
    name="test/test_soft_mux.cpp"
    classname="soft_mux.uncrustify"
  >
      <failure message="Diff with 339 lines"><![CDATA[--- test/test_soft_mux.cpp
+++ test/test_soft_mux.cpp.uncrustify
@@ -8,10 +8,11 @@
-class TestSoftMuxInterface : public::testing::Test {
-    protected:
-        std::shared_ptr<SoftMux> mux;
-        rclcpp::Subscription<custom_interfaces::msg::Pwms>::SharedPtr pwm_cmd_subscriber;
-        rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr current_control_mode_subscriber;
-        custom_interfaces::msg::Pwms most_recent_msg;
-        std_msgs::msg::Bool most_recent_mode;
-        std_msgs::msg::Bool most_recent_mux_hb;
-       
-        void SetUp() override {
+class TestSoftMuxInterface : public ::testing::Test {
+protected:
+  std::shared_ptr<SoftMux> mux;
+  rclcpp::Subscription<custom_interfaces::msg::Pwms>::SharedPtr pwm_cmd_subscriber;
+  rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr current_control_mode_subscriber;
+  custom_interfaces::msg::Pwms most_recent_msg;
+  std_msgs::msg::Bool most_recent_mode;
+  std_msgs::msg::Bool most_recent_mux_hb;
+
+  void SetUp() override
+  {
@@ -19,11 +20,13 @@
-            if (!rclcpp::ok()) {
-                rclcpp::init(0, nullptr);
-            }
-        }
-        void TearDown() override {
-            mux.reset();
-            rclcpp::shutdown();
-        }
-        void createMux() {
-            mux = std::make_shared<SoftMux>();
-        }
+    if (!rclcpp::ok()) {
+      rclcpp::init(0, nullptr);
+    }
+  }
+  void TearDown() override
+  {
+    mux.reset();
+    rclcpp::shutdown();
+  }
+  void createMux()
+  {
+    mux = std::make_shared<SoftMux>();
+  }
@@ -34,4 +37,6 @@
-        void subscribe() {
-            pwm_cmd_subscriber = mux->create_subscription<custom_interfaces::msg::Pwms>("pwm_cmd", 10, std::bind(&TestSoftMuxInterface::pwm_cmd_callback, this, std::placeholders::_1));
-        }
-        
+  void subscribe()
+  {
+    pwm_cmd_subscriber = mux->create_subscription<custom_interfaces::msg::Pwms>("pwm_cmd", 10,
+      std::bind(&TestSoftMuxInterface::pwm_cmd_callback, this, std::placeholders::_1));
+  }
+
@@ -41,5 +46,6 @@
-        void pwm_cmd_callback(custom_interfaces::msg::Pwms::UniquePtr pwm) {
-            for (int i = 0; i < 8; i++) {
-                most_recent_msg.pwms[i] = pwm->pwms[i];
-            }
-        }
+  void pwm_cmd_callback(custom_interfaces::msg::Pwms::UniquePtr pwm)
+  {
+    for (int i = 0; i < 8; i++) {
+      most_recent_msg.pwms[i] = pwm->pwms[i];
+    }
+  }
@@ -50,3 +56,5 @@
-        void subscribe_control_mode() {
-            current_control_mode_subscriber = mux->create_subscription<std_msgs::msg::Bool>("current_mode", 10, std::bind(&TestSoftMuxInterface::control_mode_callback, this, std::placeholders::_1));
-        }
+  void subscribe_control_mode()
+  {
+    current_control_mode_subscriber = mux->create_subscription<std_msgs::msg::Bool>("current_mode",
+      10, std::bind(&TestSoftMuxInterface::control_mode_callback, this, std::placeholders::_1));
+  }
@@ -57,3 +65,4 @@
-        void control_mode_callback(std_msgs::msg::Bool msg) {
-            most_recent_mode = msg;
-        }
+  void control_mode_callback(std_msgs::msg::Bool msg)
+  {
+    most_recent_mode = msg;
+  }
@@ -64,4 +73,7 @@
-        void subscribe_mux_heartbeat() {
-            rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr mux_heartbeat_subscriber = mux->create_subscription<std_msgs::msg::Bool>("mux_heartbeat", 10, std::bind(&TestSoftMuxInterface::mux_heartbeat_callback, this, std::placeholders::_1));
-        }
-        
+  void subscribe_mux_heartbeat()
+  {
+    rclcpp::Subscription<std_msgs::msg::Bool>::SharedPtr mux_heartbeat_subscriber =
+      mux->create_subscription<std_msgs::msg::Bool>("mux_heartbeat", 10,
+      std::bind(&TestSoftMuxInterface::mux_heartbeat_callback, this, std::placeholders::_1));
+  }
+
@@ -71,4 +83,5 @@
-        void mux_heartbeat_callback(std_msgs::msg::Bool msg) {
-            most_recent_mux_hb.data = true;
-            (void) msg;
-        }
+  void mux_heartbeat_callback(std_msgs::msg::Bool msg)
+  {
+    most_recent_mux_hb.data = true;
+    (void) msg;
+  }
@@ -82 +95 @@
-        createMux();
+    createMux();
@@ -102,6 +115,6 @@
-         msg1->pwms[i] = 2 * (i + 1);
-         expected1[i] = msg1->pwms[i];
-    }
-    for (int i = 0; i < 8; i++) {
-         msg2->pwms[i] = 8 * (i + 7);
-         expected2[i] = msg2->pwms[i];
+    msg1->pwms[i] = 2 * (i + 1);
+    expected1[i] = msg1->pwms[i];
+    }
+    for (int i = 0; i < 8; i++) {
+    msg2->pwms[i] = 8 * (i + 7);
+    expected2[i] = msg2->pwms[i];
@@ -111 +124 @@
-   
+
@@ -123,3 +136,3 @@
-   
-    for (int i = 0; i < 8; i++) {
-        EXPECT_EQ(most_recent_msg.pwms[i], expected1[i]);
+
+    for (int i = 0; i < 8; i++) {
+    EXPECT_EQ(most_recent_msg.pwms[i], expected1[i]);
@@ -143,6 +156,6 @@
-         msg1->pwms[i] = 2 * (i + 1);
-         expected1[i] = msg1->pwms[i];
-    }
-    for (int i = 0; i < 8; i++) {
-         msg2->pwms[i] = 8 * (i + 7);
-         expected2[i] = msg2->pwms[i];
+    msg1->pwms[i] = 2 * (i + 1);
+    expected1[i] = msg1->pwms[i];
+    }
+    for (int i = 0; i < 8; i++) {
+    msg2->pwms[i] = 8 * (i + 7);
+    expected2[i] = msg2->pwms[i];
@@ -152 +165 @@
-   
+
@@ -164,3 +177,3 @@
-   
-    for (int i = 0; i < 8; i++) {
-        EXPECT_EQ(most_recent_msg.pwms[i], expected2[i]);
+
+    for (int i = 0; i < 8; i++) {
+    EXPECT_EQ(most_recent_msg.pwms[i], expected2[i]);
@@ -182 +195 @@
-        executor.spin();
+      executor.spin();
@@ -192 +205 @@
-   
+
@@ -221 +234 @@
-        executor.spin();
+      executor.spin();
@@ -225 +238 @@
-    most_recent_mode.data = true; // set to be incorrect at first to verify that the message is received    
+    most_recent_mode.data = true; // set to be incorrect at first to verify that the message is received
@@ -231 +244 @@
-    
+
@@ -250 +263 @@
-    
+
@@ -265 +278 @@
-   
+
@@ -267 +280 @@
-    std_msgs::msg::Bool::UniquePtr test_heartbeat = std::make_unique< std_msgs::msg::Bool>();
+    std_msgs::msg::Bool::UniquePtr test_heartbeat = std::make_unique<std_msgs::msg::Bool>();
@@ -269,3 +282,3 @@
-   
-    subscribe();
-   
+
+    subscribe();
+
@@ -277 +290 @@
-         msg1->pwms[i] = 2 * (i + 1);
+    msg1->pwms[i] = 2 * (i + 1);
@@ -284,3 +297,3 @@
-        EXPECT_NE(most_recent_msg.pwms[i], 1500);
-    }
-   
+    EXPECT_NE(most_recent_msg.pwms[i], 1500);
+    }
+
@@ -295 +308 @@
-   
+
@@ -302 +315 @@
-         msg->pwms[i] = 1600;
+    msg->pwms[i] = 1600;
@@ -310,9 +323,9 @@
-        exec.spin_some();
-        std::this_thread::sleep_for(std::chrono::milliseconds(10));
-        if (most_recent_msg.pwms[0] == 1500) {
-            break;
-        }
-    }
-
-    for (int i = 0; i < 8; i++) {
-        EXPECT_EQ(most_recent_msg.pwms[i], 1500);
+    exec.spin_some();
+    std::this_thread::sleep_for(std::chrono::milliseconds(10));
+    if (most_recent_msg.pwms[0] == 1500) {
+      break;
+    }
+    }
+
+    for (int i = 0; i < 8; i++) {
+    EXPECT_EQ(most_recent_msg.pwms[i], 1500);
@@ -328 +341 @@
-   
+
@@ -335 +348 @@
-         msg->pwms[i] = 1600;
+    msg->pwms[i] = 1600;
@@ -343,9 +356,9 @@
-        exec.spin_some();
-        std::this_thread::sleep_for(std::chrono::milliseconds(10));
-        if (most_recent_msg.pwms[0] == 1500) {
-            break;
-        }
-    }
-
-    for (int i = 0; i < 8; i++) {
-        EXPECT_EQ(most_recent_msg.pwms[i], 1500);
+    exec.spin_some();
+    std::this_thread::sleep_for(std::chrono::milliseconds(10));
+    if (most_recent_msg.pwms[0] == 1500) {
+      break;
+    }
+    }
+
+    for (int i = 0; i < 8; i++) {
+    EXPECT_EQ(most_recent_msg.pwms[i], 1500);
@@ -361,2 +374,2 @@
-   
-    std_msgs::msg::Bool::UniquePtr test_heartbeat = std::make_unique< std_msgs::msg::Bool>();
+
+    std_msgs::msg::Bool::UniquePtr test_heartbeat = std::make_unique<std_msgs::msg::Bool>();
@@ -371 +384 @@
-         msg->pwms[i] = 1600;
+    msg->pwms[i] = 1600;
@@ -379,7 +392,7 @@
-        exec.spin_some();
-        std::this_thread::sleep_for(std::chrono::milliseconds(10));
-    }
-
-    for (int i = 0; i < 8; i++) {
-        EXPECT_NE(most_recent_msg.pwms[i], 1500);
-        EXPECT_EQ(most_recent_msg.pwms[i], 1600);
+    exec.spin_some();
+    std::this_thread::sleep_for(std::chrono::milliseconds(10));
+    }
+
+    for (int i = 0; i < 8; i++) {
+    EXPECT_NE(most_recent_msg.pwms[i], 1500);
+    EXPECT_EQ(most_recent_msg.pwms[i], 1600);
@@ -395,2 +408,2 @@
-   
-    std_msgs::msg::Bool::UniquePtr test_heartbeat = std::make_unique< std_msgs::msg::Bool>();
+
+    std_msgs::msg::Bool::UniquePtr test_heartbeat = std::make_unique<std_msgs::msg::Bool>();
@@ -405 +418 @@
-         msg->pwms[i] = 1600;
+    msg->pwms[i] = 1600;
@@ -413,7 +426,7 @@
-        exec.spin_some();
-        std::this_thread::sleep_for(std::chrono::milliseconds(10));
-    }
-
-    for (int i = 0; i < 8; i++) {
-        EXPECT_NE(most_recent_msg.pwms[i], 1500);
-        EXPECT_EQ(most_recent_msg.pwms[i], 1600);
+    exec.spin_some();
+    std::this_thread::sleep_for(std::chrono::milliseconds(10));
+    }
+
+    for (int i = 0; i < 8; i++) {
+    EXPECT_NE(most_recent_msg.pwms[i], 1500);
+    EXPECT_EQ(most_recent_msg.pwms[i], 1600);
@@ -435,2 +448,2 @@
-        exec.spin_some();
-        std::this_thread::sleep_for(std::chrono::milliseconds(10));
+    exec.spin_some();
+    std::this_thread::sleep_for(std::chrono::milliseconds(10));
@@ -455,12 +468,12 @@
-        exec.spin_some();
-        std::this_thread::sleep_for(std::chrono::milliseconds(10));
-        int k = 0.5;
-        if (std::chrono::steady_clock::now() == start + k * std::chrono::seconds(1)) {
-            mux->no_ctrl_heartbeat = false;
-            k = k + 1;
-        }
-        int i = 1;
-        if (std::chrono::steady_clock::now() == start + i * std::chrono::seconds(1)) {
-            EXPECT_TRUE(most_recent_mux_hb.data);
-            i++;
-        }
+    exec.spin_some();
+    std::this_thread::sleep_for(std::chrono::milliseconds(10));
+    int k = 0.5;
+    if (std::chrono::steady_clock::now() == start + k * std::chrono::seconds(1)) {
+      mux->no_ctrl_heartbeat = false;
+      k = k + 1;
+    }
+    int i = 1;
+    if (std::chrono::steady_clock::now() == start + i * std::chrono::seconds(1)) {
+      EXPECT_TRUE(most_recent_mux_hb.data);
+      i++;
+    }
@@ -471,5 +484,6 @@
-    int main(int argc, char** argv) {
-        ::testing::InitGoogleTest(&argc, argv);
-        return RUN_ALL_TESTS();
-    }
-#endif+int main(int argc, char ** argv)
+{
+  ::testing::InitGoogleTest(&argc, argv);
+  return RUN_ALL_TESTS();
+}
+#endif
]]></failure>
  </testcase>
  <system-out>Checked files:
* include/soft_mux.hpp
* src/soft_mux.cpp
* test/test_soft_mux.cpp</system-out>
</testsuite>
