<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4"
      main_tree_to_execute="ParamTree">
  <BehaviorTree ID="GateTask">
    <Sequence>
        <DriveToWorldWaypoint name="gate_drive"
                              world_waypoint="{@gate_drive.world_waypoint}"
                              waypoint_mask="{@gate_drive.waypoint_mask}"
                              tolerance="{@gate_drive.tolerance}"
                              hold_time="{@gate_drive.hold_time}"
                              timeout="{@gate_drive.timeout}" />
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="ReturnToGateTask">
    <Sequence>
      <DriveToWorldWaypoint name="return_drive"
                            world_waypoint="{@return_drive.world_waypoint}"
                            waypoint_mask="{@return_drive.waypoint_mask}"
                            tolerance="{@return_drive.tolerance}"
                            hold_time="{@return_drive.hold_time}"
                            timeout="{@return_drive.timeout}"
                            _description="Return to origin"/>
    </Sequence>
  </BehaviorTree>

  <BehaviorTree ID="ParamTree">
    <Sequence>
      <SubTree ID="GateTask"
               _autoremap="false"/>
      <SubTree ID="ReturnToGateTask"/>
    </Sequence>
  </BehaviorTree>

  <!-- Description of Node Models (used by Groot) -->
  <TreeNodesModel>
    <Action ID="DriveToWorldWaypoint">
      <input_port name="world_waypoint"
                  type="std::vector&lt;float&gt;">[6x1] Target position and orientation in world frame</input_port>
      <input_port name="waypoint_mask"
                  type="std::vector&lt;bool&gt;">[6x1] Specifies which states the controller must prioritize</input_port>
      <input_port name="tolerance"
                  type="std::vector&lt;float&gt;">[6x1] Allowed error margin for arrival</input_port>
      <input_port name="hold_time"
                  type="float">Time to remain within tolerance before returning SUCCESS</input_port>
      <input_port name="timeout"
                  type="float">Time to attempt the task before giving up and returning FAILURE</input_port>
    </Action>
  </TreeNodesModel>

</root>
