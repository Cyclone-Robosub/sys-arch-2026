1. run `colcon build --packages-select joystick_logger` to build only this package.
2. run `source install/setup.bash` to source the workspace.
3. run `ros2 run joystick_logger logger` to start the joystick logger node